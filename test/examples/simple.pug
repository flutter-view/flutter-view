import(package='testerdertest')

simple-page(flutter-view :page :user)
	platform-scaffold
		platform-app-bar(as='appBar')
			#title(as='title' :height='page.titleHeight') 
				| ${page.title}
		#body(as='body')
			column(const)
				| Welcome ${page.firstName}!
				#greeting Hello
				#actions.a.b
					box-decoration(const as='decoration')
					platform-button(const :on-pressed='page.tapped') Click me!!!
				#user(:child='user')

